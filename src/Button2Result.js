import React, { Component } from 'react';

const Button2Result = (props) => {  
    return (
      <div>
        <span>Current value: {props.currentValue}</span>
      </div>
    );
}
export default Button2Result;
